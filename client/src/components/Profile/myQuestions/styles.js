import { makeStyles } from '@mui/styles';


const useStyles = makeStyles({
    container: {
        maxWidth: 800,
        display: 'block',
        marginLeft: 'auto',
        marginRight: 'auto',
        marginTop: 10,
        marginBottom: 10,
        padding: 10
    }
});

export default useStyles;